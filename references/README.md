# Route reference frames

Place rectified clear-route frames here as `0.png`, `1.png`, and so on, matching
the waypoint indices in `config/route.json`. The obstruction node fails closed
when a required frame is missing.
